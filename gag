-- ⚠️ Bạn cần chỉnh lại path tới stats/leaderstats của game 99 Nights in the Forest
-- vì mình không có source chính xác game UI, giả sử nó lưu trong leaderstats["Diamonds"]

local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- Mốc kim cương để đổi acc
local DIAMOND_LIMIT = 10000  -- thay số này bằng mức bạn cần

local function checkDiamonds()
    local stats = player:FindFirstChild("leaderstats")
    if stats and stats:FindFirstChild("Diamonds") then
        return stats.Diamonds.Value
    end
    return 0
end

local function changeAccount()
    writefile(player.Name .. "_Done.txt", "Completed - Enough Diamonds")
    player:Kick("Enough Diamonds! Switching account...")
end

-- Vòng lặp check liên tục
spawn(function()
    while task.wait(5) do
        local diamonds = checkDiamonds()
        if diamonds >= DIAMOND_LIMIT then
            changeAccount()
            break
        end
    end
end)

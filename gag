local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local player = Players.LocalPlayer
local diamondsAttr = "Diamonds"

-- Hàm random target
local function newTarget()
    return math.random(1000, 1300)
end

-- Target Diamonds cho acc này
local targetDiamonds = newTarget()
print("🎯 Target Diamonds cho acc này là:", targetDiamonds)

-- Hàm kiểm tra Diamonds
local function checkDiamonds()
    local diamonds = player:GetAttribute(diamondsAttr) or 0
    if diamonds >= targetDiamonds then
        print("✅ Đạt điều kiện:", diamonds, " / Target:", targetDiamonds)

        -- Ghi file đánh dấu đã xong
        writefile(player.Name .. ".txt", "Completed-Done")

        -- Đổi acc (teleport lại game -> yummy auto sẽ login acc khác)
        TeleportService:Teleport(game.PlaceId, player)
    else
        print("❌ Chưa đủ Diamonds:", diamonds, " / Target:", targetDiamonds)
    end
end

-- Check ngay khi load
checkDiamonds()

-- Check mỗi khi Diamonds thay đổi
player:GetAttributeChangedSignal(diamondsAttr):Connect(checkDiamonds)

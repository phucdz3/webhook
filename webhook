-- ✅ Script gửi Webhook AFK Reward - dùng với getgenv().setting
-- Cấu hình Webhook URL cần được đặt trong getgenv().setting

return function()
    local HttpService = game:GetService("HttpService")
    local Players = game:GetService("Players")
    local Player = Players.LocalPlayer

    -- Lấy URL từ getgenv().setting
    local WebhookURL = getgenv().setting and getgenv().setting["Webhook Url"]
    if not WebhookURL then
        warn("[❌] Thiếu Webhook Url trong getgenv().setting!")
        return
    end

    -- Đợi GUI nhận thưởng xuất hiện
    local rewardGui
    repeat
        rewardGui = Player:FindFirstChild("PlayerGui") and Player.PlayerGui:FindFirstChild("CountDown")
        task.wait(1)
    until rewardGui and rewardGui:FindFirstChild("Frame")

    local receivedFrame = rewardGui.Frame:FindFirstChild("Recieved")
    if not receivedFrame then
        warn("[❌] Không tìm thấy khung nhận thưởng!")
        return
    end

    print("[✅] Đã tìm thấy giao diện nhận thưởng AFK!")

    local lastItemSent = ""
    local totalItems = 0

    -- Tự động chọn hàm gửi HTTP theo executor
    local requestFunc =
        (syn and syn.request) or
        (http and http.request) or
        (request) or
        (fluxus and fluxus.request) or
        (krnl and krnl.request)

    if not requestFunc then
        warn("[❌] Không tìm thấy hàm gửi Webhook phù hợp! Executor của bạn không hỗ trợ HTTP.")
        return
    end

    -- Gửi webhook theo định dạng yêu cầu
    local function sendWebhook(itemName)
        totalItems = totalItems + 1
        local data = {
            username = "AFK Rewards Bot",
            embeds = {{
                title = "🎁 Nhận được vật phẩm mới!",
                description = string.format("```\n⭐️ **%s**\n🧮 Tổng số vật phẩm đã nhận: `%d`\n👤 Người nhận: **%s**\n```",
                    itemName, totalItems, Player.Name),
                color = 65280  -- Màu sắc của Embed (màu xanh lá cây)
            }}
        }

        -- Gửi yêu cầu Webhook
        local success, err = pcall(function()
            requestFunc({
                Url = WebhookURL,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = HttpService:JSONEncode(data)
            })
        end)

        -- Kiểm tra kết quả gửi Webhook
        if success then
            print("[✅] Đã gửi item về Webhook:", itemName)
        else
            warn("[❌] Webhook lỗi:", err)
        end
    end

    -- Theo dõi phần tử mới trong ScrollingFrame
    local function onChildAdded(child)
        task.wait(0.2)
        local rewardNameLabel = child:FindFirstChild("RewardName")
        if rewardNameLabel and rewardNameLabel:IsA("TextLabel") then
            local itemName = rewardNameLabel.Text
            if itemName and itemName ~= lastItemSent then
                lastItemSent = itemName  -- Cập nhật item vừa nhận
                -- Gửi thông tin kết quả game theo định dạng yêu cầu
                sendWebhook(itemName)  -- Gửi item vừa nhận
            end
        end
    end

    -- Bắt đầu theo dõi các phần tử nhận thưởng
    local scrollingFrame = receivedFrame:FindFirstChild("ScrollingFrame")
    if scrollingFrame then
        scrollingFrame.ChildAdded:Connect(onChildAdded)
        print("[👀] Đang theo dõi item trong khu AFK...")
    else
        warn("[❌] Không tìm thấy ScrollingFrame trong Received!")
    end
end
